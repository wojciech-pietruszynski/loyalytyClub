<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.3</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>pl.pietruszynski</groupId>
    <artifactId>loyalty-club</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>loyalty-club</name>
    <description>Loyalty Program Application</description>
    <properties>
        <java.version>21</java.version>
        <frontend-maven-plugin.version>1.15.0</frontend-maven-plugin.version>
        <!-- SonarQube -->
        <sonar.projectKey>loyalty-club</sonar.projectKey>
        <sonar.host.url>http://192.168.100.150:9000</sonar.host.url>
        <sonar.coverage.jacoco.xmlReportPaths>${project.build.directory}/site/jacoco/jacoco.xml</sonar.coverage.jacoco.xmlReportPaths>
        <sonar.exclusions>
            **/config/**,
            **/LoyaltyClubApplication.java
        </sonar.exclusions>
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.6</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.11</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals><goal>prepare-agent</goal></goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals><goal>report</goal></goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.sonarsource.scanner.maven</groupId>
                <artifactId>sonar-maven-plugin</artifactId>
                <version>4.0.0.4121</version>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>build-backend</id>
        </profile>

        <profile>
            <!--
                Profil e2e: uruchamia pelny stack i testy Playwright.
                Kolejnosc: docker-compose-up, npm-install, playwright-install-chromium,
                spring-boot-start, playwright-test, spring-boot-stop.
                Uzycie: mvn verify -P e2e -DskipTests
                Uwaga: testuje wylacznie API backendu — frontend zyje w osobnym repo.
            -->
            <id>e2e</id>
            <build>
                <plugins>

                    <!-- ① Uruchom PostgreSQL przez Docker Compose -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.1.1</version>
                        <executions>
                            <execution>
                                <id>e2e-docker-compose-up</id>
                                <phase>pre-integration-test</phase>
                                <goals><goal>exec</goal></goals>
                                <configuration>
                                    <executable>docker</executable>
                                    <arguments>
                                        <argument>compose</argument>
                                        <argument>up</argument>
                                        <argument>-d</argument>
                                        <argument>--wait</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- ② Playwright: zainstaluj Node, npm i przeglądarkę PRZED startem backendu -->
                    <plugin>
                        <groupId>com.github.eirslett</groupId>
                        <artifactId>frontend-maven-plugin</artifactId>
                        <version>${frontend-maven-plugin.version}</version>
                        <configuration>
                            <workingDirectory>e2e</workingDirectory>
                            <installDirectory>target</installDirectory>
                        </configuration>
                        <executions>
                            <execution>
                                <id>e2e-install-node</id>
                                <phase>pre-integration-test</phase>
                                <goals><goal>install-node-and-npm</goal></goals>
                                <configuration><nodeVersion>v20.19.0</nodeVersion></configuration>
                            </execution>
                            <execution>
                                <id>e2e-npm-install</id>
                                <phase>pre-integration-test</phase>
                                <goals><goal>npm</goal></goals>
                                <configuration><arguments>install</arguments></configuration>
                            </execution>
                            <execution>
                                <id>e2e-playwright-install-browsers</id>
                                <phase>pre-integration-test</phase>
                                <goals><goal>npx</goal></goals>
                                <configuration>
                                    <arguments>playwright install --with-deps chromium</arguments>
                                </configuration>
                            </execution>

                            <!-- ④ Uruchom testy E2E -->
                            <execution>
                                <id>e2e-playwright-test</id>
                                <phase>integration-test</phase>
                                <goals><goal>npx</goal></goals>
                                <configuration>
                                    <arguments>playwright test</arguments>
                                    <environmentVariables>
                                        <PLAYWRIGHT_HEADED>${playwright.headed}</PLAYWRIGHT_HEADED>
                                    </environmentVariables>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- ③ Uruchom Spring Boot (blokuje do startu na porcie 8089) -->
                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>e2e-start</id>
                                <phase>pre-integration-test</phase>
                                <goals><goal>start</goal></goals>
                                <configuration>
                                    <!-- Odpytuje port 8089 co 1s, maks. 60 prób (60s) -->
                                    <wait>1000</wait>
                                    <maxAttempts>60</maxAttempts>
                                </configuration>
                            </execution>
                            <execution>
                                <id>e2e-stop</id>
                                <phase>post-integration-test</phase>
                                <goals><goal>stop</goal></goals>
                            </execution>
                        </executions>
                    </plugin>

                </plugins>
            </build>
            <properties>
                <!-- Domyslnie headless; nadpisywane przez profil e2e-display -->
                <playwright.headed></playwright.headed>
            </properties>
        </profile>

        <profile>
            <!-- Overlay dla profilu e2e: uruchamia testy z widoczna przegladarka.
                 Uzycie: mvn verify -P e2e,e2e-display -DskipTests -->
            <id>e2e-display</id>
            <properties>
                <playwright.headed>true</playwright.headed>
            </properties>
        </profile>

    </profiles>

</project>
